#!/bin/sh /etc/rc.common
# gatygo: xray-core with a subscription profile + transparent proxy (procd)
START=95
STOP=10
USE_PROCD=1

start_service() {
	[ "$(uci -q get gatygo.main.enabled)" = 1 ] || return 0
	echo "gatygo: not implemented yet" >&2
}
